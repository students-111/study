# 循迹逻辑正确性评估

## 需求 / 验收标准
- 评估当前固件中的灰度循迹 APP 是否具备实际循迹能力。
- 找出导致车辆无法循迹的代码级原因，并保留可验证的证据位置。
- 后续修复完成后，验证灰度采样、循迹外环、左右轮速度内环均按固定周期运行。

## 约束
- 保持 APP -> DAL -> BSP -> DriverLib/SysConfig 的依赖方向。

## 实施清单

1. 修改 `empty/User/CPU/scheduler.c`，移除速度环测试初始化、测试刷新和测试打印；初始化灰度、编码器、循迹 APP，并以灰度采样、编码器测速、循迹控制的顺序按 10 ms 调度；验证调度表无 `test_speed_loop` 符号且构建通过；review:true。
