# 研究发现

| 关键词 | 来源 | 摘要 | 可信度 | 状态 |
|---|---|---|---|---|
| 第四题目标 | 用户描述 | 按第三题 A→C→B→D→A 路线连续行驶四圈，中间不需要停止。 | 高 | 已确认 |
| 可复用能力 | 第三题实现 | 已具备 Key1 启动、相对 Yaw 转向、白底直行、弧线循迹、灰度条件捕线及圈末停车。 | 高 | 已确认 |
| 连续行驶规则 | 用户确认 | C、B、D 和前三圈回到 A 时均不停；仅第 4 圈到达 A 后停车。第四题优先追求更快完成。 | 高 | 已确认 |
| 节点转向许可 | 用户确认 | C/B/D 节点允许转向寻找黑线；第四题可保留必要转向/捕线状态，但取消第三题的 1 s 提示暂停。 | 高 | 已确认 |
| 节点过渡速度 | 用户确认 | 节点转向/找线时不将车速降为 0，使用可调低速连续转向；初始目标速度为 10（单位与现有速度环一致），后续经验调参。 | 高 | 已确认 |
| 待收敛项 | 速度策略范围 | 需确认“10”仅用于节点转向/找线，还是直线与弧线的全程目标速度也统一改为 10。 | 高 | 待用户确认 |
| 实现落点 | 当前工程结构 | task4 拆分为 `app_task4.[ch]`，由 `app.[ch]` 统一调度，避免 `app.c` 继续膨胀并保留 task2/task3。 | 高 | 已采用 |
| 连续过渡实现 | 既有速度 PID 与灰度快照 | 节点转向和找线采用左右轮速度 PID 目标 `10±5`，不将正常节点过渡速度设为零；C/D 捕到黑线直接循迹，未捕到才找线。 | 高 | 已采用 |
| 圈数判定 | 赛道状态 | D→A 弧线的离线事件记为完成一圈；前三次转入下一圈 A→C，第四次左右轮 PID 设为零。 | 高 | 已采用 |
| 异常边界 | 用户确认 | C/D 低速找线在 5 s 内未捕获黑线时，直接安全停车。 | 高 | 已确认 |
| B 点偶发误转向 | 代码审查 | `app_task4_gray_is_confirmed()` 原先按 APP 刷新次数累计，未判断灰度 `sequence` 是否更新；一个短暂白色样本会在同一灰度刷新周期内被重复累计到 3 次，提前触发 B→D 转向。 | 高 | 已修复，待上板验证 |
| 同类排查 | 代码审查 | task2/task3 的 `app_gray_is_confirmed()` 存在相同模式，已一并改为只消费新灰度快照。 | 高 | 已修复 |
| 入弧跨地形 | 用户实车观察与状态机分析 | 直线首次压到弧线黑线后，若姿态或位置未对准而再丢线，原状态机会直接视为 B/D 终点。现增加 300 ms 捕线期；该期间丢线只重新找当前弧线。 | 高 | 已实现，待上板验证 |
