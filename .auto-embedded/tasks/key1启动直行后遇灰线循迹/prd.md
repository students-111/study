# Key1启动直行后遇灰线循迹

## 需求 / 验收标准
- 上电初始化后，车辆保持停止。
- 按下 Key1 后，车辆进入 Yaw 直线保持模式，开始直线行驶寻找灰线。
- 直线行驶过程中，当灰度快照有效且检测到灰线时，自动切换到循迹模式。
- 切换到循迹模式后，Key1 不再作为停车键或模式轮换键使用，车辆继续按循迹逻辑运行。

## 约束
- 不修改 SysConfig、BSP、DAL 硬件资源。
- APP 层只读 DAL 灰度和按键快照，不新增快照读取函数。
- MVP 仅修改行车模式状态机，不新增停车键、二次启动、丢线重入直行等扩展逻辑。
