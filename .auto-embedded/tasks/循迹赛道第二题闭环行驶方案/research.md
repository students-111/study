# 研究发现

| 关键词 | 来源 | 摘要 | 可信度 | 状态 |
|---|---|---|---|---|
| 题目赛道 | 用户提供题图 | 路径为 A→B 直线（100 cm）→B→C 右半圆→C→D 直线→D→A 左半圆；要求每过 A/B/C/D 声光提示一次，整圈不大于 30 s。 | 高 | 已确认 |
| 现有控制能力 | `empty/User/APP` 与 `empty/User/CPU/scheduler.c` | 已有灰度循迹、编码器速度内环和 MPU6050 Yaw 直线保持；当前调度表以 10 ms 周期直接调用 `app_line_follow_refresh()`。 | 高 | 已确认 |
| 启动模式 | `app_drive_mode.c` 与 `dal_key.[ch]` | 按键启动、直线找线和模式切换逻辑已有雏形，但当前被注释，未接入调度器。 | 高 | 已确认 |
| 赛道可观测性 | 用户确认 | 仅有两段黑色半圆弧，A→B 与 C→D 均为白底无引导线；A/B/C/D 无额外标志。弧线端点可由灰度线的出现或消失识别。 | 高 | 已确认 |
| 白底直线结束判定 | 用户确认 | 白底直线段持续使用灰度传感器检测；重新识别黑线即判定抵达 B 或 D，并切换至弧线循迹。编码器不作为直线终点判据。 | 高 | 已确认 |
| 节点判定初稿 | 题图与用户确认 | B、D 由“无黑线→识别黑线”判定；C、A 可由“识别黑线→无黑线”判定。所有边沿均须经消抖、状态机使能和最小行驶时间保护。 | 中 | 待用户确认 |
| 节点提示 MVP | 用户确认 | 当前硬件不具备声光功能，暂不实现声光。每次检测到黑白状态切换（进入黑线或离开黑线）均停车 1 s，作为节点提示；暂停结束后再进入下一行驶状态。 | 高 | 已确认 |
| 启动条件 | 用户确认 | 小车在 A 点就位，按下 Key1 后才开始执行首段 A→B 白底直行。 | 高 | 已确认 |
| 路径连续性 | 题图几何 | A→B、B→C、C→D、D→A 在各节点的运动切线方向连续，不需要原地掉头或额外转向。 | 中 | 待实物确认 |
| 时间预算 | 题图与需求 | 两段直线各 100 cm，右/左半圆半径分别为 60/40 cm 时，全程约 5.14 m。四次暂停合计 4 s，运动时间须不大于 26 s，平均车速至少约 19.8 cm/s 才能满足 30 s 限制。 | 中 | 待实测 |
| 边沿确认基础 | `dal_gray.[ch]` | 灰度快照以 10 ms 周期刷新，提供 `line_detected`、`active_count` 和 `sequence`；APP 可基于连续快照实现进入/离开黑线的确认，无需修改 DAL。 | 高 | 已确认 |
| 暂停时基基础 | `bsp_time.h` | BSP 已提供可自然溢出的毫秒计数接口，APP 可用其实现非阻塞的 1 s 暂停，无需阻塞延时。 | 高 | 已确认 |
| 模块落点候选 | 现有 APP 结构 | 候选一：扩展现有 `app_drive_mode` 管理赛题状态，复用直线/循迹 APP；候选二：新增赛题 APP 文件。前者改动面更小且职责匹配，推荐前者。 | 高 | 待用户确认 |
| 起步灰度状态 | 用户确认 | 小车在 A 点起步时灰度传感器不会检测到黑线；Key1 后直接等待 B 点黑线出现，不存在起步离线事件或额外暂停。 | 高 | 已确认 |
| 灰度确认参数 | 用户确认 | 进入黑线和离开黑线均以连续 3 次灰度刷新结果一致作为确认条件；按 10 ms 刷新周期，首版确认延迟约 30 ms。 | 高 | 已确认 |
| 状态机模块落点 | 用户指定 | 新建 `empty/User/APP/app.c`（配套 `app.h`）承载赛题状态机；`app_drive_mode.c` 暂不使用。循迹与直线 APP 仅做必要最小修改。 | 高 | 已确认 |
| Key1 与灰度引脚迁移 | 用户确认 | Key1 固定引脚由 PA18 改为 PB21；为消除冲突，GRAY_D2 由 PB21 改为 PB0。 | 高 | SysConfig 已生成验证 |
| Key1 有效电平 | Key 原理图 | PB21 经按键直接接 GND，未见外部上拉；SysConfig 配置内部上拉，`dal_key` 按低电平判定为按下。 | 高 | 已生成并核对 |
| SysConfig 端口宏收敛 | Keil 构建日志与生成头文件 | Key1 从 PA18 迁到 PB21 后，`BOARD_GPIO` 组所有引脚均位于 GPIOB；SysConfig 因此只生成组级 `BOARD_GPIO_PORT`，不再生成各功能的 `*_PORT`。所有 BSP/DAL 引用已改为该生成宏。 | 高 | 已构建验证 |
| 构建与门禁 | Keil / `check.py` | 使用 `E:\Kill_5\UV4\UV4.exe` 重建成功：0 error、0 warning；ARCH、HW、SPEC 全部通过。 | 高 | 已验证 |
